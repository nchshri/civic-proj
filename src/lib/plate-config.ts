import {
  createParagraphPlugin,
  ELEMENT_PARAGRAPH,
} from '@udecode/plate-paragraph';

export const initialValue = [
  {
    type: ELEMENT_PARAGRAPH,
    children: [{ text: 'Start typing... Type /rewrite to transform text!' }],
  },
];